# 设计模式概述

## 访问者模式描述

`访问者模式` ，是一种行为设计模式，它将算法与其作用的对象隔离开来。它要解决的核心事项是，在一个稳定的数据结构下，例如用户信息、雇员信息等，增加一边的业务访问逻辑。为了增强扩展性，将两部分的业务解耦的一种设计模式。

说人话，访问者模式的核心在于同一个事务不同视角下的访问信息不同，比如：一个 `美女` 手里拿着 `冰激凌`。小朋友会注意冰激凌，大朋友会找自己喜欢的地方“细细观察”。

## 与其他设计模式的关系：

- 你可以将[访问者]()视为[命令模式]()的加强版本，其对象可对不同 类的多种对象执行操作。 
- 你可以使用访问者对整个组合树执行操作。 
- 可以同时使用**访问者**和**迭代器**来遍历复杂数据结构，并对其中的元素执行所需操作，即使这些元素所属的类完全不同。

## 业务场景

在本案例中我们通过 `模拟校园中的学⽣和⽼师对于不同⽤户的访问视⻆` 的场景。

## 模块含义

* 00：场景模拟工程；模拟学生和老师信息不同视角访问。